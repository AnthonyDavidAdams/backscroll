"""Backscroll -- a local, searchable memory of everything on your screen.

Open-source, zero-dependency, macOS-first. Backscroll continuously captures
your screen, runs on-device OCR, and stores the text in a local SQLite
database with full-text search. The AI that queries that history is your own
Claude Code / Claude subscription, via the built-in MCP server (`backscroll
serve`) -- no third-party API keys, no cloud, nothing leaves your machine.
"""

__version__ = "0.1.0"
